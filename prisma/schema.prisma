generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    String @id
  email String @unique
}

model Artist {

  name String
}

model Ticket {
  id            String     @id
  title         String
  collection    Collection @relation(fields: [collection_id], references: [id])
  collection_id String
  claimee       User? @relation(fields: [user_id], references[id])
  claimee_id    String?
}

model Collection {
  id        String   @id
  slug      String   @unique @db.VarChar(20)
  name      String
  tickets   Ticket[]
  latitude  Float
  longitude Float
}
